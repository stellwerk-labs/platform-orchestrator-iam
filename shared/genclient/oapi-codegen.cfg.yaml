# Config used for generating the API models and server stubs based on the openapi/spec.yaml
package: genclient
generate:
  models: true
  client: true
  echo-server: false
compatibility:
  headers-implicitly-required: true
output: shared/genclient/client.gen.go
